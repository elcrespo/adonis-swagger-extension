## adonis-swagger-extension

The package has been installed and configured successfully.
You can view your Swagger docs at: `http://localhost:3333/docs`

Make sure to adjust the `typesPaths` array in your `config/swagger.ts` file to point to all your source paths where Type and Interface declarations reside!
